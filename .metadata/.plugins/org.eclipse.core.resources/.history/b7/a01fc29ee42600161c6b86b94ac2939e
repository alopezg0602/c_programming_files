/*
 ============================================================================
 Name        : Excersice_1.c
 Author      : Aldo
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include<conio.h>


void ordAscending(int *ptr_vector, int n);
void displayArray(int *ptr_to_array,int i);
void exerciseCode_0(void);

#define EXERCISE_LIST\
	  /**********(Exercise Description)****/       \
	DISPLAY_LIST("Exercise 1: Compare two numbers")\
	DISPLAY_LIST("Exercise 2: Even or Odd")\
	DISPLAY_LIST("Exercise 3: Compare three numbers" )\

#define FUNCTION_LIST\
	  /*********(ID , Function)*/\
	DISPLAY_LIST(0,exerciseCode_0)\
	DISPLAY_LIST(1,exerciseCode_1)\
	DISPLAY_LIST(2,exerciseCode_2)\

#define DISPLAY_LIST(label) \
		printf(label);\



int main(void) {
	int selector=0;
	printf("Choose one program from the below list to be executed: \n");
	EXERCISE_LIST
	getch();
	return 0;
}

void ordAscending(int *ptr_vector, int n)
{
	int interruptor = 1;
	int *ptr_store=ptr_vector;
	int *ptr_aux=ptr_vector;
	ptr_aux++;
	int mem=0;

	int pasada, j;
	/*ordering cycle*/
	for(pasada = 0; ((pasada < n-1) && interruptor); pasada++)
	{
		interruptor = 0;
		for (j=0; j<n-pasada -1;j++)
		{
			if(*ptr_vector > *(ptr_aux))
			{
				//change if are not in the required sequence
				interruptor = 1;
				mem=*ptr_vector;
				*ptr_vector = *ptr_aux;
				*ptr_aux=mem;
			}
			ptr_vector++;
		    ptr_aux++;
		}
		ptr_vector=ptr_store;
		ptr_aux=ptr_vector;
		ptr_aux++;
	}
}

void displayArray(int *ptr_to_array,int i)
{
	for(i=0;i<5;i++)
	{
		printf("%d ", *ptr_to_array);
		ptr_to_array++;
	}
	printf("\n");
}

void exerciseCode_0(void)
{
	int i=5;
	int a[5]={20,40,50,30,80};
	printf("Arreglo Inicial 2 \n");
	displayArray(&a[0],i);
	ordAscending(&a[0],i);
	printf("\n Arreglo Final 2 \n");
	displayArray(&a[0],i);
}
